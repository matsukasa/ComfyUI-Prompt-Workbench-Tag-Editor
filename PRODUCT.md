# Product

## Register

product

## Platform

web

## Users

大量のPrompt WorkbenchタグをWindows上で整理するComfyUIユーザー。JSONを直接編集せず、タグと3階層カテゴリの関係を視覚的に確認・変更したい人を主対象とする。

## Product Purpose

Prompt Workbenchが利用するタグカタログをローカルだけで読み込み、複数選択、階層横断移動、カテゴリ編集、検証、差分確認を行い、確認付き上書きまたは別名のJSONとして安全に保存する。

## Positioning

3階層と未知メタデータを維持したまま数千タグを視覚的に整理し、Prompt Workbenchと同じ上書き・別名保存フローを使える専用エディター。

## Brand Personality

精密、カラフル、落ち着き。ComfyUIの暗い作業環境になじみながら、カテゴリ色を操作上の手掛かりとして積極的に使う。

## Anti-references

装飾目的のグラデーション、ガラス表現、過度に丸いカード、巨大な指標、絵文字依存、色だけで状態を伝えるUI、元ファイルを自動保存する挙動は採用しない。

## Design Principles

- 移動元、移動先、挿入位置を常に同時に示す。
- 密度を維持しながら、色と階層で迷いを減らす。
- ドラッグと同じ操作をキーボードとボタンでも提供する。
- 不明なデータは削除せず、危険な書き出しは止める。
- 標準的なWindowsの複数選択操作を尊重する。

## Accessibility & Inclusion

キーボード操作、明確なフォーカス、Ctrl／Shift選択、Escキャンセル、aria属性、ドラッグ代替ボタン、色以外の状態表現、reduced-motionに対応する。
